<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Biblioteca de PDFs</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    body {
      font-family: "Segoe UI", sans-serif;
      background: linear-gradient(135deg, #181c1d, #000000);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 30px;
      color: #333;
    }
    .container {
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0,0,0,0.2);
      padding: 30px;
      max-width: 600px;
      width: 100%;
    }
    h1 {
      text-align: center;
      margin-bottom: 20px;
      color: #2193b0;
    }
    form {
      display: flex;
      justify-content: space-between;
      margin-bottom: 30px;
      flex-wrap: wrap;
    }
    input[type="file"] {
      flex: 1 1 auto;
      margin-right: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      padding: 8px;
    }
    button {
      background: #2193b0;
      color: #fff;
      border: none;
      padding: 10px 20px;
      border-radius: 4px;
      cursor: pointer;
      transition: background 0.3s;
    }
    button:hover {
      background: #17667a;
    }
    .pdf-list {
      margin-top: 20px;
    }
    .pdf-list h2 {
      margin-bottom: 10px;
      font-size: 1.2em;
      color: #2193b0;
    }
    .pdf-list a {
      display: block;
      background: #f1f1f1;
      padding: 10px;
      margin: 5px 0;
      border-radius: 4px;
      text-decoration: none;
      color: #333;
      transition: background 0.3s;
    }
    .pdf-list a:hover {
      background: #ddd;
    }
    p {
      text-align: center;
      color: #555;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Biblioteca de PDFs</h1>
    
    <form action="/upload" method="POST" enctype="multipart/form-data">
      <input type="file" name="pdfFile" accept=".pdf" required>
      <button type="submit">Enviar PDF</button>
    </form>
    
    <div class="pdf-list">
      <h2>Livros Disponíveis:</h2>
      <% if (uploads.length > 0) { %>
        <% uploads.forEach(function(upload) { %>
          <a href="/download/<%= upload.savedName %>" target="_blank">
            <%= upload.originalName %>
          </a>
        <% }); %>
      <% } else { %>
        <p>Nenhum PDF enviado ainda.</p>
      <% } %>
    </div>
  </div>
</body>
</html>
